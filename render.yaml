services:
  - type: web
    name: ai-hub-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: PINECONE_API_KEY
        sync: false
      - key: PINECONE_INDEX
        value: aihub
      - key: GITHUB_TOKEN
        sync: false   # optional – add your GitHub PAT for live triage
      - key: ETSY_API_KEY
        sync: false   # optional – for live Etsy API
